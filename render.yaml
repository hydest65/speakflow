services:
  - type: web
    name: speakflow
    env: node
    buildCommand: "npm install"
    startCommand: "npm start"
    pullRequestPreviewsEnabled: true
    envVars:
      - key: OPENAI_MODEL
        value: gpt-5-mini
      - key: OPENAI_API_KEY
        sync: false
